func abs(x) {
    if x < 0 {
        -x 
    } else {
        x
    }
}

func sign(x) {
    if x < 0 {
        -1
    } else {
        0
    }
}

func double(x) {
    x + x
}

func min(x, y) {
    if x < y {
        x
    } else {
        y
    }
}

func max(x, y) {
    if x > y {
        x
    } else {
        y
    }
}

private {
    func id(x) {
        x
    }
}

func userName(name) {
    "User " + name
}

type Foobar(x, y) with Lookup

type Foo(x) with Lookup

let pi = 3.14