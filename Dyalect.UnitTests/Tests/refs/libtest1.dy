import libtest

let PI = 3.14

type Asset = Computer(model, memory, rank) | Phone(model, memory) | Modem(model)

func sayHello() {
    "Hello, \(userName("Jon"))"
}

func double(x) {
    "double " + x
}