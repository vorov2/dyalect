/*func fin(z) {
    z / 0
}

func zoo(y) {
    var z = y + y / 2
    fin(z)
}

func bar(x) {
    var y = x * 2
    zoo(y)
}

func foo() {
    var x = 12
    bar(x)
}

foo() 

func foo(x = 142, y, z) {
    print("x=", x, ";y=", y, ";z=", z)
}

func print(args..., separator = ",", terminator = "\n") {
    var str = ""

    for x in args.iter() {
        if str.len() > 0 {
            str += separator
        }
        str += x.toString()
    }

    str += terminator
    str
}

print(1,2,3,separator:" ",terminator:".")
*/


var x = 42
var y = 12

"the value of (x + y) is \(x + y)"
