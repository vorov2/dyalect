/*func fin(z) {
    z / 0
}

func zoo(y) {
    var z = y + y / 2
    fin(z)
}

func bar(x) {
    var y = x * 2
    zoo(y)
}

func foo() {
    var x = 12
    bar(x)
}

foo() 

func foo(x = 142, y, z) {
    print("x=", x, ";y=", y, ";z=", z)
}

func print(args..., separator = ",", terminator = "\n") {
    var str = ""

    for x in args.iter() {
        if str.len() > 0 {
            str += separator
        }
        str += x.toString()
    }

    str += terminator
    str
}

print(1,2,3,separator:" ",terminator:".")
*/

var arr = [4,6,1,3,2,5]
arr.sort((x,y) => x - y)
assert([1, 2, 3, 4, 5, 6], arr)