/*func fin(z) {
    z / 0
}

func zoo(y) {
    var z = y + y / 2
    fin(z)
}

func bar(x) {
    var y = x * 2
    zoo(y)
}

func foo() {
    var x = 12
    bar(x)
}

foo() 
*/

static func Array.empty(size, item) {
    var i = 0
    const arr = []
    while i < size {
        arr.add(item)
        i += 1
    }
    arr
}

String.concat("one", "two", "three")
