import libtest

let pi = 3.14

type Asset = Computer(model, memory, rank) or Phone(model, memory) or Modem(model)

func sayHello() {
    "Hello, \(userName('J'+'o'+'n'))"
}

func double(x) {
    "double " + x
}