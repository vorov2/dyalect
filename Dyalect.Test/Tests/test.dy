/*func Integer + (n) {
    self * n
}

Integer: func toString(){
    "[" + self + "]"
}

var x = 12.0;

x:multiply(12, 3)

x multiply(12, 3)

if x > 0 {
    x *= 12.3456
}

{
    var x = "Yeah-whooo!";
    print(x)
}


func Float multiply(x,y) {
    var res = self * (x - y);

    if res < 0 {
        return -res;
    }
    else {
        return res;
    }
}

var x = 12.0

x.multiply(34, 26)
*/

var arr = [Float] 

func multiply(x,y)
{
    var res = self * (x - y);

    if res < 0 {
        return -res;
    }
    else {
        return res;
    }
}