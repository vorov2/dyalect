/*func test1() {
    func sorter(x, y) {
        if x > 10 {
            throw "No!"
        }
        x - y
    }
    var arr = [1..11]
    var ex
    try {
        arr.sort(sorter)
    } catch e {
        ex = e
    }
    assert("No!", ex)
}

test1()
*/

if var (x, y, z) = (1, 2) {
    print(x)
} else {
    print("It didn't match and x is null")
}