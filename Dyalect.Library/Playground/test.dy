/*func fin(z) {
    z / 0
}

func zoo(y) {
    var z = y + y / 2
    fin(z)
}

func bar(x) {
    var y = x * 2
    zoo(y)
}

func foo() {
    var x = 12
    bar(x)
}

foo() 

func foo(x = 142, y, z) {
    print("x=", x, ";y=", y, ";z=", z)
}

func print(args..., separator = ",", terminator = "\n") {
    var str = ""

    for x in args.iter() {
        if str.len() > 0 {
            str += separator
        }
        str += x.toString()
    }

    str += terminator
    str
}

print(1,2,3,separator:" ",terminator:".")



var i = 42
var n = 0

while n < 42 {
    if isPrime(i) {
        n += 1
        print("n = \(n) \(i)")
        i += i - 1
    }
    i += 1
}

func isPrime(number) {
    if number <= 1 {
        return false
    }
    else if number % 2 == 0 {
        return number == 2
    }

    var i = 3

    while (i * i) < number {
        if number % i == 0 {
            return false
        }
        i += 2
    }

    return true
}

func testPrimes() {
    var i = 42
    var n = 0

    while n < 42 {
        if isPrime(i) {
            n += 1
            print("n = \(n)\t\(i)")
            i += i - 1
        }
        i += 1
    }
}

func testFunCall() {
    func sum(x,y,z) {x+y+z}

    var iter = 1000*1000

    while iter > 0 {
        sum(1,2,3)
        iter -= 1
    }
}

func testIter() {
    var xs = 1..(1000*1000)

    const result =
        for x in xs {
            if x >= 1000*1000 {
                break x
            }
        }

    result
}

*/
