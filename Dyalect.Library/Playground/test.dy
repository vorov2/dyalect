type Foo

static func Foo.new(x) {
    x
}

func Foo.toString() {
    "Foo" + this + "!"
}

auto func Foo.name() {
    "Name=\(this)"
}

var f = Foo.new(12)
//f.name

var x = 2.0 / 0
assert(true, x == Float.inf)